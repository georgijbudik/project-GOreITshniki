import styled from 'styled-components';
import { StyledButton } from 'components/Button/Button.styled';

export const StyledToggleLanguageButton = styled(StyledButton)`
  z-index:100;
  padding: 10px;
  font-size: 12px;
`;
